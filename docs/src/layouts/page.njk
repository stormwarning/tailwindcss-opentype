---
layout: base
---

{% from 'page-header.njk' import header with context %}
{% from 'class-table.njk' import table with context %}

{{ header(title=title, description=description, category=tags) }}

<div class="prose dark:prose-invert mt-10">
	{% if classData %}
		{{ table(classes[classData]) }}
	{% endif %}

	{{ content | safe }}
</div>
