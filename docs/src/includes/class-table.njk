<div class="border-b border-gray-200 overflow-hidden relative">
    <span class="sr-only">Default class reference</span>
    <div
        class="overflow-y-auto scrollbar-w-2 scrollbar-track-gray-lighter scrollbar-thumb-rounded scrollbar-thumb-gray scrolling-touch lg:max-h-sm"
    >
        <table class="w-full text-left border-collapse">
            <thead>
                <tr>
                    <th class="z-20 sticky top-0 text-sm font-semibold text-gray-600 bg-white p-0">
                        <div class="pb-2 pr-2 border-b border-gray-200">Class</div>
                    </th>
                    <th class="z-20 sticky top-0 text-sm font-semibold text-gray-600 bg-white p-0">
                        <div class="pb-2 pl-2 border-b border-gray-200">
                            Properties
                        </div>
                    </th>
                </tr>
            </thead>
            <tbody class="align-baseline">

                <tr>
                    <td
                        class="py-2 pr-2 font-mono text-xs text-violet-600 whitespace-nowrap"
                    >
                          {# {
                            'border-t border-gray-200': i !== 0,
                          } #}
                        {# {transformSelector(selector)} #}
                    </td>
                    <td class="py-2 pl-2 font-mono text-xs text-light-blue-600 whitespace-pre">
                          {# {
                            'border-t border-gray-200': i !== 0
                          } #}
                        {# {stringifyProperties(transformProperties({ selector, properties }), {
                          filter: filterProperties,
                          transformValue,
                        })} #}
                    </td>
                </tr>

            </tbody>
        </table>
    </div>
</div>
